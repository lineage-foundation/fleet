# Trivy: repository (vuln + misconfiguration) and built container image.
# Fails the job on CRITICAL or HIGH severity findings (see `.trivyignore` for justified exceptions).
name: Trivy

on:
  pull_request:
    paths:
      - "Dockerfile"
      - "docker-compose.yml"
      - ".docker/**"
      - ".github/workflows/trivy.yml"
      - ".trivyignore"
      - "Cargo.lock"
      - "Cargo.toml"
  push:
    branches: [main]
    paths:
      - "Dockerfile"
      - "docker-compose.yml"
      - ".docker/**"
      - ".github/workflows/trivy.yml"
      - ".trivyignore"
      - "Cargo.lock"
      - "Cargo.toml"
  workflow_dispatch:

concurrency:
  group: trivy-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Opt into Node.js 24 for JS actions ahead of GHES runner defaults (checkout, docker helpers, third-party actions).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  IMAGE_TAG: fleet-node:ci
  TRIVY_SEVERITY: CRITICAL,HIGH

jobs:
  trivy-repo:
    name: Trivy filesystem (repo)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Run Trivy (FS — vuln, misconfiguration)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: "."
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: "1"
          scanners: vuln,misconfig
          trivyignores: .trivyignore
          skip-dirs: target,.git,run,tasks,src/db/db,src/wallet/wallet,src/bin/tls_data
          # Same PEM-in-JSON fixtures as image; not exercised in current deployments.
          skip-files: src/bin/tls_certificates.json,.docker/conf/tls_certificates.json

  trivy-image:
    name: Docker build + Trivy image
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - name: Build image (linux/amd64)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          load: true
          tags: ${{ env.IMAGE_TAG }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy (container image)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: image
          image-ref: ${{ env.IMAGE_TAG }}
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: "1"
          trivyignores: .trivyignore
          # PEM material in bundled JSON placeholder; TLS not used for this image path (see Dockerfile → /etc/tls_certificates.json).
          skip-files: /etc/tls_certificates.json
