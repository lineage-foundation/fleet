[workspace]
members = ["crates/fleet", "crates/fleet-core", "crates/fleet-node-common", "crates/fleet-wallet"]
resolver = "2"

# Faster dev links on Linux and macOS (ignored on platforms that do not support it).
[profile.dev]
split-debuginfo = "unpacked"
